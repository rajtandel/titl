# Copy to ".env" in the repository root (same folder as package.json).

# MongoDB — Atlas SRV string or local: mongodb://127.0.0.1:27017/titl
MONGODB_URI=

# Signing key for session JWTs (min 16 characters). Example:
# JWT_SECRET=change-me-use-openssl-rand-base64-32
JWT_SECRET=
